const initModel = {
  nextId: 1,
  cards: [
    {
      question : 'What is a pure function?',
      answer : 'A pure function must always return the same output given the same input, and its execution must not depend on the outside state. It must not cause any side effects.',
      displayMode: 'question',
      rank: 0,
      id: 0,
    },
    {
      question : 'What is a pure function?',
      answer : 'A pure function must always return the same output given the same input, and its execution must not depend on the outside state. It must not cause any side effects.',
      displayMode: 'question',
      rank: 0,
      id: 0,
    },
    {
      question : 'What is a pure function?',
      answer : 'A pure function must always return the same output given the same input, and its execution must not depend on the outside state. It must not cause any side effects.',
      displayMode: 'question',
      rank: 0,
      id: 0,
    },
    {
      question : 'What is a pure function?',
      answer : 'A pure function must always return the same output given the same input, and its execution must not depend on the outside state. It must not cause any side effects.',
      displayMode: 'question',
      rank: 0,
      id: 0,
    },
    {
      question : 'What is a pure function?',
      answer : 'A pure function must always return the same output given the same input, and its execution must not depend on the outside state. It must not cause any side effects.',
      displayMode: 'question',
      rank: 0,
      id: 0,
    },
    {
      question : 'What is a pure function?',
      answer : 'A pure function must always return the same output given the same input, and its execution must not depend on the outside state. It must not cause any side effects.',
      displayMode: 'question',
      rank: 0,
      id: 0,
    },
  ],
};

export default initModel;
